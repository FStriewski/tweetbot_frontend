import * as React from 'react';
import { BottomBar as StyledBottomBar } from '../styles/BottomBar';

const BottomBar = () => (
  <div>
    <StyledBottomBar>BottomBar</StyledBottomBar>
  </div>
);

export default BottomBar;
